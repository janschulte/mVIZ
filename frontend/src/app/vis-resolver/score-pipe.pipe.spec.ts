import { ScorePipePipe } from './score-pipe.pipe';

describe('ScorePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ScorePipePipe();
    expect(pipe).toBeTruthy();
  });
});
